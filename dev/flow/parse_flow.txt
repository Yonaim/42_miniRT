
토큰 타입: 단어(word), 숫자(number), 콤마(,), 괄호(parentheses), 해쉬(#)

1. 들어온 하나의 line에 대해 공백을 기준으로 토큰화하여 토큰 리스트를 생성한다.
(하나의 토큰은 토큰 타입, 문자열 시작 주소, 길이를 가진다)

2. 식별자에 따라 알맞은 info 구조체 생성 함수를 호출한다.
	- 범위 오류시 에러 처리

4. info 구조체를 받아 최종적으로 해당 요소를 생성(혹은 값 할당)한다.
	(이때, solid라면 object array에 넣어야 한다)
